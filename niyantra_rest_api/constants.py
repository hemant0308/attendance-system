
class Permissions:
    authenticated_only = 'authenticated_only'
    admin_only = 'admin_only'
    every_one = 'every_one'

class Roles:
    Admin='Admin'

EMPTY_STRING = ''
DEFAULT_PERMISSION = Permissions.authenticated_only
TEACHER_DEFAULT_PASSWORD = 'password'

